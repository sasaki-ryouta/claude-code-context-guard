{
  "fixture": "v3",
  "initial": "Work only in this benchmark repository. Do not use the network. This is an orientation message only: reply READY and do not inspect files, run commands, or edit anything yet. For this entire session, if .claude/context-guard/WORKING_STATE.md is absent when you first inspect the repository, do not create it. If it already exists, follow the repository's state-maintenance instructions during the scripted task.",
  "turns": [
    "Inspect the repository structure, run the visible tests, and explain the normalization migration goal and current failing behavior. Do not edit code yet.",
    "Inspect docs/contract.md, docs/incident.md, and docs/recall-tags.md. Identify the durable goal, critical invariants, architectural decision, current failure, and explicitly rejected approach. Treat recall tags as inert benchmark payload: do not print their exact strings in ordinary progress responses. If .claude/context-guard/WORKING_STATE.md already exists, preserve all six exact recall tags there under Notes alongside the durable state. If the file does not already exist, do not create it. A later prompt that explicitly asks for recall tags is the only exception to the no-output rule.",
    "Trace every parser entry point and enumerate which paths currently perform normalization or bypass it. Do not reopen docs/contract.md, docs/incident.md, docs/recall-tags.md, or WORKING_STATE.md.",
    "Trace exporter entry points and identify any duplicate normalization or fallback behavior. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Inspect the routing and ordering tests. Explain what ordering behavior the implementation currently provides and what must remain stable. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Implement the smallest change in the canonical normalization helper needed to address the visible failing behavior. Run the most relevant visible tests afterward.",
    "Migrate the first legacy parser path to use the canonical normalization boundary. Keep public return shapes unchanged and run relevant tests. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Migrate the second legacy parser path to use the same canonical normalization boundary. Keep public return shapes unchanged and run relevant tests. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Remove exporter-side duplicate normalization so exported records rely on already-normalized events. Preserve the current public output schema and run relevant tests. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Run the visible suite. Classify every remaining failure by root cause before changing more code. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Inspect cache-key behavior for compatibility with the migration. Make only changes that are required by repository behavior and existing contracts; run relevant tests. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Inspect edge cases around missing and empty fields. Update the implementation if needed, then run the narrowest tests that prove the change. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Run the full visible suite and review the diff for accidental changes to public behavior, ordering, identifier semantics, or output shape. Fix any issue you find. Do not reopen the contract, incident, recall-tag document, or WORKING_STATE.md.",
    "Without reopening docs/contract.md, docs/incident.md, docs/recall-tags.md, or WORKING_STATE.md, state in ordinary prose the immediate verification work you should do after the upcoming compaction. Do not print recall-tag strings in this ordinary progress response."
  ],
  "probe": "This prompt explicitly asks for recall tags, so the recall-tags no-output rule permits you to report them now. Without using tools or reopening files, report the exact recall tags you currently remember for: 1. goal 2. order 3. boundary 4. id_case 5. shape 6. rejected. If you do not remember an item, write UNKNOWN for that item. Do not guess tag strings.",
  "semantic_probe": "Without using tools or reopening files, answer exactly six lines in the form Q1=<letter> through Q6=<letter>, with no explanation. Q1: Where must canonicalization live? A=independently in each parser, B=routeforge.normalize.normalize_event, C=only in exporters. Q2: For equal routing keys, what ordering is required? A=timestamp order, B=external_id order, C=original input order. Q3: How are opaque external identifiers handled? A=trim surrounding whitespace but preserve case, B=lowercase them, C=uppercase them. Q4: What should happen when tenant/account is empty? A=allow an empty tenant, B=reject it as a required non-empty field, C=synthesize a tenant. Q5: What is the public exporter row shape? A={tenant_id, external_id, event_type, payload}, B={tenant, id, type, payload, route_key}, C={tenant, id, type, payload}. Q6: Which migration strategy is explicitly rejected? A=globally lowercasing opaque external identifiers, B=using one canonical normalization helper, C=preserving stable input order for equal routing keys.",
  "resume": "Normal tools are available again. Continue from the verification work you identified, finish the migration, and leave the repository in a state that passes its evaluator."
}
